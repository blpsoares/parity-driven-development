{
  "name": "parity-driven-development",
  "version": "2.1.0",
  "description": "PDD — Parity-Driven Development. A framework for reliable legacy refactor/rewrite/port with tracked behavioral parity. Ships the `pdd` dashboard CLI (runs on Node or Bun — no npm needed).",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild scripts/pdd/index.ts --bundle --platform=node --format=esm --target=node18 --outfile=dist/pdd.js --banner:js=\"#!/usr/bin/env node\"",
    "pdd": "bun run scripts/pdd/index.ts",
    "test": "bun test scripts/pdd"
  },
  "author": "Bryan Soares",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
