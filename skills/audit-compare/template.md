# Parity diff — {{TITLE}}

**Finding**: NNN-{{SLUG}} — see [`../README.md`](./README.md)
**Reference case**: {{REFERENCE_CASE}}
**Generated by**: /audit-compare
**Date**: {{DATE}}
**Execution mode**: {{MODE}}   <!-- CLI | DB query (MCP) | API call | browser (MCP) | visual-fallback (tier-1) -->
**Evidence tier**: {{TIER}}    <!-- tier-2 when a diff was produced; tier-1 when visual fallback -->

## Systems

| Side | System | How it was reached | Operation run |
|---|---|---|---|
| Reference | {{REFERENCE_NAME}} | {{REFERENCE_ACCESS}} | `{{REFERENCE_COMMAND}}` |
| New | {{NEW_NAME}} | {{NEW_ACCESS}} | `{{NEW_COMMAND}}` |

<!--
REFERENCE_COMMAND / NEW_COMMAND = the exact CLI command, DB query, API request,
or browser navigation executed on each side. They MUST be the semantically SAME
operation. Raw captures live next to this file (parity-reference.raw / parity-new.raw).
Normalizations applied (timestamps/ids stripped, collections sorted): {{NORMALIZATIONS}}
-->

## Diff (reference → new)

```diff
{{DIFF_BODY}}
```

<!--
Empty diff → replace {{DIFF_BODY}} with:  NO DIFFERENCES — parity confirmed
Non-empty  → unified/field-level diff. Lines prefixed - = reference value,
             + = new-system value. Keep it deterministic (sort unordered sets first).
-->

## Summary

{{SUMMARY}}

<!--
- Empty diff:  "Parity confirmed for <case>. Finding qualifies for tier-2."
- Non-empty:   list the differing fields/lines (reference value vs new value) and
               point to /audit-resolve to close the gap.
- Visual fallback: note tier-1, list the paired screenshots, and the limitation
  that blocked executing the reference system.
-->
